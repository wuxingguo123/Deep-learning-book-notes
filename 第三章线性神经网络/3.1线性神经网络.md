# 3.1. 线性回归  

## 3.1.1. 线性回归的基本元素  

### 3.1.1.1. 线性模型

线性假设是指目标（房屋价格）可以表示为特征（面积和房龄）的加权和，如下面的式子：  

$$price=w_1\cdot a_1+w_2\cdot a_2+...w_n\cdot a_n+b$$

$$\text{price} = \sum_{i=1}^{n} w_i \cdot a_i + b$$  

其中 $w_i$ 为权重， $a_i$ 为输入样本的第 $i$ 个特征。
